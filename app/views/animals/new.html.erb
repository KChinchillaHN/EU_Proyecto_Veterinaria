<h1>Crear un nuevo Tipo de Animal</h1>
<p><em>Ingrese los siguientes datos: </em></p>

<%= render partial: "shared/errors", locals: { obj: @animal } %>

<%= form_for @animal do |f| %>
<%= render partial: "form", locals: {f: f} %>
	<p>
		<%= f.submit "Crear" %>
		<%= link_to "Cancelar", animals_path %>
	</p>
<%- end %>