<tr>
	<td><%= animal.id %></td>
	<td><%= animal.name %></td>
	<td><%= link_to edit_animal_path(animal) do %>
		<span class="fa fa-edit"></span>
		Editar Animal
	<% end %>
	</td>
	<td><%= link_to "Eliminar", animal, method: :delete, data: {confirm: "Estas Seguro?"}  %> </td>
</tr>